{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Basaltic-Red","text":"<p> Zero-copy Data Lake and data-quality engine in Rust + Apache Arrow (Python via PyO3) </p>","path":["Home - Basaltic-Red"],"tags":[]},{"location":"#overview","level":2,"title":"Overview","text":"<p><code>basaltic-red</code> is a Rust core for file-based data lakes with a thin Python layer. It provides: a memory-mapped catalog (<code>.br_map.ipc</code>), row/column slicing without full reads, parallel quality filtering with per-row audit codes, and DataFusion SQL over Arrow batches. Results are <code>pyarrow.Table</code> / <code>RecordBatch</code> for downstream use with Polars, DuckDB, pandas.</p> <p>Demo in <code>demo.ipynb</code>: NYC TLC Yellow Taxi 2009–2025 — 204 Parquet files, 29.66 GB, 1,826,960,642 rows × 20 columns.</p> <pre><code>graph LR\n    A[\"Raw files (204 Parquet / 29.66 GB)\"] --&gt; B[\"Rust core\"]\n    B --&gt; C[\"Catalog (.br_map.ipc / memmap2)\"]\n    B --&gt; D[\"Parallel filter (Rayon, 1.82B rows)\"]\n    B --&gt; E[\"DataFusion SQL stream\"]\n    D --&gt; F[\"Clean (1.78B)\"]\n    D --&gt; G[\"Trash (46.7M + audit codes)\"]\n    E --&gt; H[\"DuckDB / Polars\"]</code></pre>","path":["Home - Basaltic-Red"],"tags":[]},{"location":"#what-it-does","level":2,"title":"What it does","text":"<ul> <li>Catalog (<code>.br_map.ipc</code>): one Arrow IPC file at the lake root; warm reads via <code>memmap2</code> avoid a directory walk. Doctor reports <code>HEALTHY</code> / <code>DRIFT_DETECTED</code> / <code>HEALED</code>.</li> <li>Slicing: <code>slice_rows</code> / <code>slice_cols</code> read only the requested rows/columns.</li> <li>Filtering: dynamic rules (<code>col op value</code>, <code>op ∈ {&gt;=, &lt;=, ==, !=, &gt;, &lt;}</code>) evaluated per batch; trash rows carry <code>audit_error_code</code> (<code>u64</code> bitmask, chunked for &gt;64 rules) and <code>audit_violated_rules</code> when needed.</li> <li>SQL: DataFusion session; <code>execute_sql_stream</code> returns a <code>PyBatchIterator</code> whose <code>to_pyarrow()</code> hands batches to Polars/DuckDB without an extra copy.</li> <li>Formats: pluggable delimited formats via <code>br.formats.register_delimited</code>; extension-less files handled by magic-byte sniffing.</li> </ul>","path":["Home - Basaltic-Red"],"tags":[]},{"location":"#demo-measurements","level":2,"title":"Demo measurements","text":"<p>Single run of <code>demo.ipynb</code> on Apple Silicon / macOS. Hardware-dependent — indicative only.</p> Scenario Scope Observed Catalog cold vs warm 204 files Cold build ~18.07 s → warm ~0.5 ms (avg 5 runs) Volume scan (metadata only) 1,826,960,642 rows &lt; 1 s (~0.7 s) Full-lake filter (5 rules) 1,826,960,642 rows ~21 s (1,780,228,507 clean / 46,732,135 trash) Single-file SQL <code>GROUP BY</code> 4,305,006 rows ~0.1 s <p>Filtering loops are plain Rust over Arrow arrays; LLVM auto-vectorizes them. No hand-written SIMD intrinsics.</p>","path":["Home - Basaltic-Red"],"tags":[]},{"location":"#getting-started","level":2,"title":"Getting started","text":"<pre><code>uv add \"git+https://github.com/VanDung-dev/basaltic-red.git\"\n</code></pre> <pre><code>import basaltic_red as br\n\nreport = br.lake.doctor(\"data/\", auto_heal=True)\nprint(report[\"status\"], report[\"total_files\"])\n\nclean, trash = br.filter.filter_matrix(\"data/sample.parquet\", rules=[\n    \"passenger_count &gt;= 1\",\n    \"trip_distance &gt; 0.0\",\n    \"fare_amount &gt; 0.0\",\n])\n</code></pre> <p>Full walkthrough: Quickstart · Lake Doctor · Benchmarks</p>","path":["Home - Basaltic-Red"],"tags":[]},{"location":"changelog/","level":1,"title":"Changelog","text":"","path":["Other","Changelog"],"tags":[]},{"location":"changelog/#unreleased","level":2,"title":"Unreleased","text":"<p>Only changes reflected in the current <code>src/</code> tree.</p> Improvements (30) <ul> <li> <p>2026-08-21</p> <ul> <li>Lake Map: Data-lake indexing with consistency checks, <code>create_map</code> / <code>doctor</code> Python functions, and zero-copy memory-mapped catalog loading.</li> </ul> </li> <li> <p>2026-08-18</p> <ul> <li>PyAPI: Introduced <code>PyBatchIterator</code> with enhanced SQL engine integration.</li> </ul> </li> <li> <p>2026-08-17</p> <ul> <li>Formats: Magic-byte file format sniffing capability.</li> </ul> </li> <li> <p>2026-08-16</p> <ul> <li>Formats: Base templates for delimited reading and row chunking; plugin modules organizing adapters and templates into tiers.</li> </ul> </li> <li> <p>2026-08-15</p> <ul> <li>Engine: Multi-chunk SIMD bitmasking in <code>filter_batch_dynamic</code>; additional hints for non-Parquet data detection.</li> <li>Formats &amp; PyAPI: Dynamic custom format handler registration exposed through Python registration functions.</li> </ul> </li> <li> <p>2026-08-12</p> <ul> <li>Engine: Schema-based row byte estimation and batch sizing derived from the first file's schema.</li> <li>SQL &amp; PyAPI: Auto-normalize cache for non-native SQL files; lazy streaming execution for <code>PyBatchIterator</code>.</li> </ul> </li> <li> <p>2026-08-11</p> <ul> <li>Engine: Enhanced memory budgeting with a global Rayon pool; DataFusion native listing table support for file querying; one-time hint when reading non-Parquet files.</li> <li>Ingest: Parallelized file ingestion with target conflict handling.</li> </ul> </li> <li> <p>2026-08-10</p> <ul> <li>JSON: Top-level JSON arrays streamed as bare object streams.</li> <li>Ingest: Native directory ingestion with optional normalization to Parquet, exposed through the Python API with a batch-size recommendation module.</li> <li>Runtime: Global tokio runtime and unified row batch budgeting.</li> </ul> </li> <li> <p>2026-08-08</p> <ul> <li>Readers: Column projection pushed into CSV and Parquet readers.</li> <li>PyAPI: New pyapi modules with migrated function modules.</li> <li>Engine: Matrix data processing performance optimizations.</li> </ul> </li> <li> <p>2026-08-06</p> <ul> <li>SQL: Streaming execution with iterators.</li> <li>PyAPI: Mutex-guarded <code>PyBatchIterator</code> source with a PyArrow conversion bridge.</li> <li>Utils: Sorted file listing in the discovery utility.</li> </ul> </li> <li> <p>2026-08-05</p> <ul> <li>Security: CSV injection guard sanitizing dangerous cells.</li> <li>Formats: Unified handling behind <code>FormatHandler::open</code>; native <code>orc-rust</code> reader replacing the Parquet-based path (<code>orc-rust</code>, <code>rust_xlsxwriter</code> dependencies).</li> <li>Engine: <code>clamp_batch_size</code> integrated into all handlers; improved input validation for file paths.</li> <li>Build: <code>[lib]</code> section enabling Python extension and Rust library builds.</li> </ul> </li> <li> <p>2026-08-04</p> <ul> <li>Filtering: Multi-threaded <code>filter_files_parallel</code> with Hive-style partition pruning.</li> </ul> </li> <li> <p>2026-08-03</p> <ul> <li>Engine: Slicing, filtering, splitting, and ER diagram generation driven by dynamic column rules.</li> </ul> </li> <li> <p>2026-08-02</p> <ul> <li>Formats: XLSX, Avro, Feather, ORC, and MsgPack processing support with dependencies for these formats.</li> </ul> </li> <li> <p>2026-08-01</p> <ul> <li>Formats: TXT and PSV file processing support.</li> </ul> </li> <li> <p>2026-07-30</p> <ul> <li>Engine: <code>MatrixEngine</code> core with processing, filtering, and Parquet streaming; <code>filter_batch_native</code> batch filtering with error auditing; CSV/TSV/JSON/Parquet and NDJSON/JSONL streaming; sample preview and data dictionary export; audit error-code bitmask constants.</li> <li>Utils: Data-file discovery utilities with filtering options.</li> <li>PyAPI: PyO3 Python bindings for <code>MatrixEngine</code>.</li> </ul> </li> </ul> Fix (8) <ul> <li> <p>2026-08-20</p> <ul> <li>Engine: Hardened file handling and regex initialization.</li> <li>Engine (IO): Reused file handles with reader state reset after schema inference instead of reopening files.</li> <li>SQL Cache: Invalidated cached Parquet files when the source modification time is newer than the cache.</li> <li>PyAPI: Released the GIL around async blocking calls (<code>py.detach</code>) in lazy stream sources.</li> </ul> </li> <li> <p>2026-08-18</p> <ul> <li>Build: Corrected <code>PyBatchIterator</code> module export placement.</li> </ul> </li> <li> <p>2026-08-17</p> <ul> <li>Engine: Improved error handling and file path resolution.</li> </ul> </li> <li> <p>2026-08-12</p> <ul> <li>Ingest: Restricted <code>ingest_normalize</code> visibility to the crate (<code>pub(crate)</code>).</li> </ul> </li> <li> <p>2026-08-11</p> <ul> <li>Engine: Consistent non-Parquet hints across slice and extension-resolution paths with clearer error messages.</li> </ul> </li> </ul>","path":["Other","Changelog"],"tags":[]},{"location":"glossary/","level":1,"title":"Glossary","text":"<p>Quick lookup only. For explanations, follow the linked component pages.</p>","path":["Reference","Glossary"],"tags":[]},{"location":"glossary/#source-map","level":2,"title":"Source Map","text":"Term Location One-line meaning <code>MatrixEngine</code> <code>src/engine/mod.rs</code> Central struct holding quality thresholds Engine bindings <code>src/pyapi/engine.rs</code> <code>#[pymethods]</code> exposed to Python <code>default_engine()</code> <code>src/pyapi/mod.rs</code> Process-wide engine singleton (<code>OnceLock</code>) <code>BazanError</code> <code>src/error.rs</code> Error enum; mapped by <code>bazan_to_pyerr()</code> Static filter <code>src/engine/filter.rs</code> Fixed-threshold fast path (<code>filter_batch_native</code>) Audit bit flags <code>src/filter.rs</code> <code>ERR_INVALID_PASSENGER</code> / <code>_FARE</code> / <code>_SPEED</code> Dynamic kernel <code>src/engine/dynamic_filter.rs</code> <code>FilterRule::parse</code> + multi-chunk bitmask Slicing <code>src/engine/slice.rs</code> <code>slice_rows_native</code>, <code>slice_cols_native</code> Parallel filter <code>src/engine/parallel_filter.rs</code> Rayon multi-file run, <code>ParallelFilterSummary</code> Partition pruning <code>src/engine/partition.rs</code> Hive-style path parsing &amp; file pruning Splitter <code>src/engine/splitter.rs</code> <code>split_file_native</code> part writer Ingest <code>src/engine/ingest.rs</code> Directory ingestion, Parquet normalization Lake writers <code>src/engine/formats/core/parquet.rs</code> Clean/trash lake write, gold table, ZSTD SQL layer <code>src/engine/sql.rs</code> DataFusion session, ListingTable vs MemTable, <code>.br_cache</code> <code>PyBatchIterator</code> <code>src/pyapi/iterator.rs</code> Eager/Lazy batch source, <code>to_pyarrow()</code> Format trait <code>src/engine/formats/mod.rs</code> <code>FormatHandler</code>, registries, magic-byte sniffer Tier 1–3 handlers <code>formats/core/</code>, <code>common/</code>, <code>plugins/adapters/</code> Parquet/Feather · CSV &amp; JSON families · XLSX/Avro/ORC/MsgPack Row chunking <code>formats/plugins/base_templates/row_chunker.rs</code> Shared row→batch conversion template Lake Map <code>src/engine/map.rs</code> <code>LakeMap</code>, <code>.br_map.ipc</code> IO, <code>doctor_lake_map</code> Memory budget <code>src/engine/memory.rs</code> RAM cap, batch sizing, tokio/Rayon runtimes CSV Guard <code>src/engine/csv_guard.rs</code> Formula-injection sanitizer ER graphs <code>src/engine/graph.rs</code> Mermaid ER diagram generator File discovery <code>src/utils.rs</code> Recursive, sorted, partition-aware walkers","path":["Reference","Glossary"],"tags":[]},{"location":"glossary/#vocabulary","level":2,"title":"Vocabulary","text":"Term Definition RecordBatch Arrow columnar chunk — unit of streaming everywhere <code>OpenedSource</code> Schema + lazy batch iterator returned by a handler Clean / Trash Rows passing all rules vs violating ≥1 rule Bitmask chunk 64 rules per <code>u64</code>; rule i → bit <code>i % 64</code> of chunk <code>i / 64</code> <code>audit_error_code</code> <code>UInt64</code> bitmask of violated rules 0–63 on Trash rows <code>audit_violated_rules</code> <code>List&lt;UInt32&gt;</code> of all violated indices (rules &gt; 64 only) <code>.br_map.ipc</code> Arrow IPC catalog at lake root (5 columns, see spec) Doctor status <code>HEALTHY</code> / <code>DRIFT_DETECTED</code> / <code>HEALED</code> Drift classes <code>modified_files</code>, <code>unindexed_files</code>, <code>missing_files</code> Partition filter Hive-style subtree selector, e.g. <code>year=2026/month=08</code> Summary dict <code>{total_files, pruned_dirs, total_rows, clean_rows, trash_rows}</code> Native target Extension DataFusion reads directly (pushdown enabled) MemTable fallback Non-native formats loaded to RAM before querying","path":["Reference","Glossary"],"tags":[]},{"location":"glossary/#environment-variables","level":2,"title":"Environment Variables","text":"Variable Default Effect <code>BASALTIC_RED_MAX_RAM_GB</code> <code>2</code> Total streaming RAM budget <code>BR_INGEST_NORMALIZE</code> off <code>1</code>/<code>true</code> normalizes row formats during ingest <code>BASALTIC_RED_AUTO_NORMALIZE</code> off <code>1</code> enables the SQL-side transcode cache <code>BASALTIC_RED_CACHE_DIR</code> <code>&lt;dir&gt;/.br_cache</code> Relocates the SQL transcode cache","path":["Reference","Glossary"],"tags":[]},{"location":"architecture/datafusion/","level":1,"title":"DataFusion SQL Layer","text":"<p><code>src/engine/sql.rs</code> embeds Apache DataFusion. Both commands accept a single SQL string where the <code>FROM</code> clause references a file path or directory (single-quoted):</p> <pre><code>import basaltic_red as br\n\ntable  = br.sql.execute_sql(\"SELECT COUNT(*) FROM 'data/analytics'\")       # collected Table\nstream = br.sql.execute_sql_stream(\"SELECT * FROM 'data/analytics'\")       # PyBatchIterator\n</code></pre>","path":["Components","DataFusion SQL Layer"],"tags":[]},{"location":"architecture/datafusion/#target-resolution","level":2,"title":"Target Resolution","text":"<p>Before planning, the engine registers the <code>FROM</code> target as a table named <code>br_target</code>. Two paths exist:</p> <ol> <li> <p>Native ListingTable — for extensions DataFusion reads itself:</p> Extension DataFusion format <code>parquet</code>, <code>pq</code> ParquetFormat <code>csv</code>, <code>tsv</code>, <code>psv</code> CsvFormat (delimiter-aware) <code>json</code>, <code>jsonl</code>, <code>ndjson</code> JsonFormat (newline-delimited objects) <code>arrow</code>, <code>ipc</code>, <code>feather</code> ArrowFormat <p>A directory of homogeneous extension registers as one ListingTable, enabling predicate &amp; projection pushdown across all files.</p> </li> <li> <p>Fallback MemTable — for formats without a native DataFusion reader (<code>xlsx</code>, <code>avro</code>, <code>orc</code>, <code>msgpack</code>, mixed directories) and for JSON files whose top level is an array (<code>[...]</code>). The file is read through the format registry, collected into a MemTable, then queried in memory.</p> </li> </ol> <p>Top-level JSON arrays</p> <p>DataFusion's <code>JsonFormat</code> expects newline-delimited objects. Files starting with <code>[</code> are detected by inspecting the first non-whitespace byte and routed to the streaming handler instead.</p>","path":["Components","DataFusion SQL Layer"],"tags":[]},{"location":"architecture/datafusion/#streaming-vs-collecting","level":2,"title":"Streaming vs Collecting","text":"Command Return Memory profile <code>execute_sql(query)</code> PyArrow <code>Table</code> Fully collected <code>execute_sql_stream(query)</code> <code>PyBatchIterator</code> Lazy — batches produced on demand","path":["Components","DataFusion SQL Layer"],"tags":[]},{"location":"architecture/datafusion/#pybatchiterator-srcpyapiiteratorrs","level":3,"title":"PyBatchIterator (<code>src/pyapi/iterator.rs</code>)","text":"<p>Two internal sources behind one class:</p> <ul> <li>Lazy — a live <code>SendableRecordBatchStream</code>; each <code>next()</code> pulls one batch from the tokio runtime (<code>memory::global_runtime()</code>).</li> <li>Eager — a pre-collected <code>Vec&lt;RecordBatch&gt;</code> (used when a MemTable fallback was required).</li> </ul> <p>Python-facing API:</p> <ul> <li>iteration: <code>for batch in stream:</code> yields PyArrow RecordBatches</li> <li><code>stream.to_pyarrow()</code> → complete PyArrow Table</li> <li><code>repr(stream)</code> → <code>PyBatchIterator(batches=N, rows=M)</code> (counts known eagerly; filled during consumption for lazy streams)</li> </ul> <p>The output feeds directly into Polars / DuckDB with no copy — see Integrate with Polars &amp; DuckDB.</p>","path":["Components","DataFusion SQL Layer"],"tags":[]},{"location":"architecture/datafusion/#what-gets-pushed-down","level":2,"title":"What Gets Pushed Down","text":"<p>On native ListingTables, DataFusion decodes only the row groups and columns your plan needs:</p> <pre><code># Reads only passenger_count + fare_amount column chunks, prunes row groups by predicate\nbr.sql.execute_sql_stream(\n    \"SELECT passenger_count, AVG(fare_amount) \"\n    \"FROM 'data/output/clean_trips.parquet' GROUP BY passenger_count\"\n)\n</code></pre>","path":["Components","DataFusion SQL Layer"],"tags":[]},{"location":"architecture/filtering-pipeline/","level":1,"title":"Filtering Pipeline","text":"<p>Two entry points share the same bitmask kernel:</p> <ul> <li><code>filter_matrix(file_path, rules)</code> — one file, returns <code>(clean_table, trash_table)</code>.</li> <li><code>filter_files_parallel(path_pattern, rules)</code> — many files via Rayon, returns a summary dict.</li> </ul>","path":["Components","Filtering Pipeline"],"tags":[]},{"location":"architecture/filtering-pipeline/#single-file-flow-engine-pyapienginersfilter_matrix","level":2,"title":"Single-File Flow (<code>engine</code> + <code>pyapi/engine.rs::filter_matrix</code>)","text":"<pre><code>sequenceDiagram\n    autonumber\n    participant PY as Python (filter_matrix)\n    participant RP as FilterRule::parse\n    participant FR as formats registry (handler_for)\n    participant ST as OpenedSource stream\n    participant KD as filter_batch_dynamic\n\n    PY-&gt;&gt;RP: rule strings list[str]\n    RP--&gt;&gt;PY: parsed FilterRule vec (col · op · value)\n    PY-&gt;&gt;FR: resolve handler for extension\n    FR--&gt;&gt;ST: lazy RecordBatch iterator\n    loop every batch within RAM budget\n        ST-&gt;&gt;KD: RecordBatch\n        KD--&gt;&gt;PY: clean batch + trash batch (+ audit bitmask)\n    end\n    PY-&gt;&gt;PY: concat_batches → (clean_table, trash_table)</code></pre> <p>Filtering runs batch-by-batch so peak memory stays at batch budget, not file size.</p>","path":["Components","Filtering Pipeline"],"tags":[]},{"location":"architecture/filtering-pipeline/#multi-file-parallel-filter-engineparallel_filterrs","level":2,"title":"Multi-File Parallel Filter (<code>engine/parallel_filter.rs</code>)","text":"<ol> <li>Target collection — <code>collect_target_files()</code> accepts a single file path, a directory (walked recursively), or a glob pattern (<code>*</code>, <code>?</code>, <code>[...]</code>). Directories are walked with partition awareness.</li> <li>Partition pruning — for Hive-style layouts (<code>year=2026/month=08/...</code>), <code>parse_path_partitions()</code> extracts key/value pairs from each path and <code>matches_partition_rules()</code> drops whole files before opening them. Pass an explicit filter like <code>\"year=2026/month=08\"</code> or rules on partition columns.</li> <li>Rayon execution — surviving files are filtered across the global Rayon pool; optional <code>num_threads</code> overrides pool width.</li> <li>Summary — counts are reduced into a dict:</li> </ol> <pre><code>summary = br.filter.filter_files_parallel(\n    \"data/yellow_tripdata_2025-*.parquet\",\n    rules=[\"passenger_count &gt; 0\", \"fare_amount &gt;= 2.5\"],\n)\n# {\n#   \"total_files\": 12,\n#   \"pruned_dirs\": 0,      # directories skipped entirely by partition pruning\n#   \"total_rows\": 51_660_072,\n#   \"clean_rows\":  50_112_004,\n#   \"trash_rows\":  1_548_068,\n# }\n</code></pre> <p>Parallel mode writes no output</p> <p><code>filter_files_parallel</code> is a counting pass — it reports how many rows would survive. To materialize clean/trash Parquet outputs per directory, use <code>br.lake.process_and_write_lake</code>.</p>","path":["Components","Filtering Pipeline"],"tags":[]},{"location":"architecture/filtering-pipeline/#memory-behavior","level":2,"title":"Memory Behavior","text":"<p>Batch sizes adapt to the RAM budget: each of N concurrent streams gets <code>budget_batch_rows(N)</code> rows so total in-flight rows stay bounded (~2 GB by default, tunable via <code>BASALTIC_RED_MAX_RAM_GB</code>). See <code>src/engine/memory.rs</code>.</p>","path":["Components","Filtering Pipeline"],"tags":[]},{"location":"architecture/formats/","level":1,"title":"Format Registry &amp; Magic-Byte Sniffing","text":"<p>Every file access in <code>basaltic-red</code> resolves to a <code>FormatHandler</code> — the pluggable abstraction in <code>src/engine/formats/mod.rs</code>.</p>","path":["Components","Format Registry & Magic-Byte Sniffing"],"tags":[]},{"location":"architecture/formats/#the-formathandler-trait","level":2,"title":"The <code>FormatHandler</code> Trait","text":"<pre><code>pub struct OpenedSource {\n    pub schema: SchemaRef,\n    pub batches: Box&lt;dyn Iterator&lt;Item = Result&lt;RecordBatch, BazanError&gt;&gt; + Send&gt;,\n}\n\npub trait FormatHandler: Send + Sync {\n    fn open(&amp;self, file_path: &amp;str, batch_size: usize) -&gt; Result&lt;OpenedSource, BazanError&gt;;\n    fn process_file(&amp;self, engine: &amp;MatrixEngine, file_path: &amp;str, batch_size: usize)\n        -&gt; Result&lt;(usize, usize, usize), BazanError&gt;;\n    fn read_range(&amp;self, file_path: &amp;str, offset: usize, limit: usize, batch_size: usize)\n        -&gt; Result&lt;RecordBatch, BazanError&gt;;\n    fn open_with_columns(&amp;self, file_path: &amp;str, batch_size: usize, columns: &amp;[String])\n        -&gt; Result&lt;OpenedSource, BazanError&gt;;\n    fn read_range_columns(&amp;self, /* ... */) -&gt; Result&lt;RecordBatch, BazanError&gt;;\n}\n</code></pre> <p>Handlers return a lazy <code>OpenedSource</code> — an Arrow schema plus a streaming batch iterator. Row-based formats (CSV, JSONL, XLSX) chunk their rows through shared templates in <code>plugins/base_templates/row_chunker.rs</code>; columnar formats (Parquet, IPC) stream natively.</p>","path":["Components","Format Registry & Magic-Byte Sniffing"],"tags":[]},{"location":"architecture/formats/#built-in-formats","level":2,"title":"Built-in Formats","text":"<p>Static table <code>HANDLERS</code> maps extensions to handlers:</p> Tier Extensions Handler source 1 — Core <code>parquet</code>, <code>pq</code>, <code>feather</code>, <code>arrow</code>, <code>ipc</code> <code>formats/core/parquet.rs</code>, <code>formats/core/arrow_ipc.rs</code> 2 — Common <code>csv</code>, <code>tsv</code>, <code>psv</code>, <code>txt</code>, <code>json</code>, <code>jsonl</code>, <code>ndjson</code> <code>formats/common/csv.rs</code>, <code>formats/common/json.rs</code> 3 — Pluggable adapters <code>xlsx</code>, <code>avro</code>, <code>orc</code>, <code>msgpack</code> <code>formats/plugins/adapters/</code> <p>Delimited variants share one template (<code>plugins/base_templates/delimited.rs</code>) differing only in delimiter byte and header flag.</p>","path":["Components","Format Registry & Magic-Byte Sniffing"],"tags":[]},{"location":"architecture/formats/#handler-resolution-order","level":2,"title":"Handler Resolution Order","text":"<p><code>resolve_handler_for_file()</code> tries, in order:</p> <ol> <li>Extension lookup (<code>handler_for</code>) — O(1), checks the dynamic registry first, then the static table. Extension case is normalized to lowercase.</li> <li>Magic-byte sniffing (<code>sniff_format_from_file</code>) — reads the first 512 bytes and inspects them via <code>sniff_format_from_bytes</code>:</li> </ol> Header signature Resolved format <code>PAR1</code> <code>parquet</code> <code>ARROW1</code> <code>feather</code> <code>PK\\x03\\x04</code> (Zip) <code>xlsx</code> <code>Obj\\x01</code> <code>avro</code> <code>ORC</code> <code>orc</code> <code>0x80–0x8F / 0xDE / 0xDF</code> (MessagePack map) <code>msgpack</code> first non-space byte <code>[</code> <code>json</code> first non-space byte <code>{</code> <code>ndjson</code> UTF-8 text line containing <code>\\t</code> / <code>\\|</code> / <code>;</code> / <code>,</code> <code>tsv</code> / <code>psv</code> / <code>txt</code> / <code>csv</code> <p>This is why files without any extension still open correctly.</p>","path":["Components","Format Registry & Magic-Byte Sniffing"],"tags":[]},{"location":"architecture/formats/#dynamic-registration-from-python","level":2,"title":"Dynamic Registration from Python","text":"<p>The dynamic registry lives behind a <code>RwLock&lt;HashMap&lt;String, Arc&lt;dyn FormatHandler&gt;&gt;&gt;</code> and is manipulated via <code>br.formats.*</code>:</p> <pre><code>import basaltic_red as br\n\nbr.formats.register_delimited(ext=\"dat\", delimiter=\"|\", has_header=True)\nprint(br.formats.list_formats())     # includes \"dat\" among the built-ins\ntable = br.read.slice_rows(\"data/custom.dat\", offset=0, limit=50)\nbr.formats.unregister_format(\"dat\")  # returns True if it existed\n</code></pre> <p>Registered handlers override built-ins for the same extension.</p>","path":["Components","Format Registry & Magic-Byte Sniffing"],"tags":[]},{"location":"architecture/lake-map/","level":1,"title":"Binary Lake Map &amp; Lake Doctor","text":"<p>Implemented in <code>src/engine/map.rs</code>. The Lake Map replaces recursive filesystem walks with a single pre-compiled Arrow IPC file — <code>.br_map.ipc</code> — stored at the root of the data lake.</p>","path":["Components","Binary Lake Map & Lake Doctor"],"tags":[]},{"location":"architecture/lake-map/#catalog-lifecycle","level":2,"title":"Catalog Lifecycle","text":"<pre><code>stateDiagram-v2\n    direction LR\n    state \"Building catalog\" as Building\n    state \".br_map.ipc saved\" as Saved\n    state \"HEALTHY\" as Healthy\n    state \"DRIFT_DETECTED\" as Drift\n    state \"HEALED\" as Healed\n\n    [*] --&gt; Building: br.lake.create_map()\n    Building --&gt; Saved: save_lake_map_ipc()\n    Saved --&gt; Healthy: doctor · entries match\n    Saved --&gt; Drift: doctor · modified / unindexed / missing\n    Healthy --&gt; Drift: files change on disk\n    Drift --&gt; Healed: doctor(auto_heal=True)\n    Healed --&gt; Healthy: catalog in sync again</code></pre> <ul> <li><code>build_lake_map()</code> walks the directory (via <code>discover_data_files</code>), reads each file's schema/row count and per-column min/max stats.</li> <li><code>save_lake_map_ipc()</code> serializes the map; <code>load_lake_map_ipc()</code> reads it back through a memory map.</li> </ul>","path":["Components","Binary Lake Map & Lake Doctor"],"tags":[]},{"location":"architecture/lake-map/#on-disk-schema","level":2,"title":"On-Disk Schema","text":"Column Arrow Type Description <code>rel_path</code> <code>Utf8</code> Path relative to the lake root <code>size_bytes</code> <code>UInt64</code> File size <code>mtime_ms</code> <code>Int64</code> Modification time in milliseconds since Unix epoch <code>total_rows</code> <code>UInt64</code> Row count <code>stats_json</code> <code>Utf8</code> JSON blob: per-column <code>{min, max, min_str, max_str}</code> plus row count <p>The aggregate struct also carries <code>total_files</code>, <code>total_rows</code>, <code>total_bytes</code>.</p>","path":["Components","Binary Lake Map & Lake Doctor"],"tags":[]},{"location":"architecture/lake-map/#lake-doctor","level":2,"title":"Lake Doctor","text":"<p><code>doctor_lake_map(dir_path, auto_heal)</code> compares the on-disk reality against the catalog:</p> Report field Meaning <code>status</code> <code>\"HEALTHY\"</code> | <code>\"DRIFT_DETECTED\"</code> | <code>\"HEALED\"</code> <code>total_files</code> Files seen on disk <code>healthy_count</code> Entries matching the catalog exactly (path + size + mtime) <code>modified_files</code> Known paths whose size/mtime changed <code>unindexed_files</code> New files missing from the catalog <code>missing_files</code> Indexed files no longer on disk <code>healed</code> Whether healing ran <p>Healing rebuilds the entry list from what still exists (dropping <code>missing_files</code>, refreshing stats for modified/unindexed entries) and rewrites <code>.br_map.ipc</code>. Status becomes <code>\"HEALED\"</code>. Without <code>auto_heal=True</code> the report is purely diagnostic.</p> <pre><code>import basaltic_red as br\n\nreport = br.lake.doctor(\"data\", auto_heal=False)\nif report[\"status\"] != \"HEALTHY\":\n    report = br.lake.doctor(\"data\", auto_heal=True)\n</code></pre> <p>Full command signatures: <code>br.lake.*</code> · binary layout details: Lake Map Specification.</p>","path":["Components","Binary Lake Map & Lake Doctor"],"tags":[]},{"location":"architecture/lakehouse-pipeline/","level":1,"title":"Lakehouse Pipeline","text":"<p>The write-side modules of <code>src/engine/</code>: <code>ingest.rs</code>, <code>splitter.rs</code>, <code>formats/core/parquet.rs</code> (lake + gold writers), <code>partition.rs</code>, and <code>memory.rs</code>.</p>","path":["Components","Lakehouse Pipeline"],"tags":[]},{"location":"architecture/lakehouse-pipeline/#ingest-brlakeingest","level":2,"title":"Ingest (<code>br.lake.ingest</code>)","text":"<p>Copies a source directory into a destination lake, preserving relative layout:</p> <ul> <li>Row-based formats (<code>csv, tsv, psv, txt, json, jsonl, ndjson, msgpack, xlsx</code>) are converted to Parquet when normalization is on — either via <code>auto_normalize=True</code> or the <code>BR_INGEST_NORMALIZE=1</code> environment variable.</li> <li>Everything else is copied byte-for-byte.</li> </ul> <p>Returns <code>(files_ingested, rows_ingested)</code>.</p>","path":["Components","Lakehouse Pipeline"],"tags":[]},{"location":"architecture/lakehouse-pipeline/#split-brlakesplit_file","level":2,"title":"Split (<code>br.lake.split_file</code>)","text":"<p><code>split_file_native()</code> streams a file and writes fixed-size parts:</p> <pre><code>n = br.lake.split_file(\"big.csv\", max_rows_per_file=100_000,\n                       output_dir=\"./parts\", format=\"parquet\")  # → number of parts\n</code></pre> <p>Output format can be any writable extension (e.g. <code>parquet</code>, <code>csv</code>). Parts are named <code>&lt;stem&gt;_part_NNN.&lt;format&gt;</code>.</p>","path":["Components","Lakehouse Pipeline"],"tags":[]},{"location":"architecture/lakehouse-pipeline/#cleantrash-lake-write-brlakeprocess_and_write_lake","level":2,"title":"Clean/Trash Lake Write (<code>br.lake.process_and_write_lake</code>)","text":"<p>Reads every Parquet file under <code>input_dir</code> in parallel (optionally narrowed by a Hive-style <code>partition_filter</code>), applies the static quality thresholds per batch, and writes two Parquet trees that mirror the input's relative layout:</p> <pre><code>sequenceDiagram\n    autonumber\n    participant IN as input_dir (Parquet files)\n    participant FL as static threshold filter\n    participant CL as clean_output_dir/\n    participant TR as trash_output_dir/\n\n    IN-&gt;&gt;FL: each RecordBatch (Rayon parallel)\n    FL-&gt;&gt;CL: passing rows (ZSTD Parquet)\n    FL-&gt;&gt;TR: violating rows + audit_error_code\n    FL--&gt;&gt;IN: (total_files, total_rows, clean_rows, trash_rows)</code></pre> <p>Returns <code>(total_files, total_rows, clean_rows, trash_rows)</code>. Trash files carry the same <code>audit_error_code</code> column as dynamic filtering. All parts are written with ZSTD compression; batch size adapts to the number of concurrent files.</p>","path":["Components","Lakehouse Pipeline"],"tags":[]},{"location":"architecture/lakehouse-pipeline/#gold-table-brlakegenerate_gold_table","level":2,"title":"Gold Table (<code>br.lake.generate_gold_table</code>)","text":"<p>Reads a clean Parquet directory back and republishes it as a versioned table:</p> <pre><code>files, gold_rows, manifest = br.lake.generate_gold_table(\n    \"clean/\", \"gold/\", table_version=\"v1\",\n)\n</code></pre> <ul> <li>Output paths mirror <code>clean/</code>'s relative layout under <code>gold/</code> (ZSTD compressed).</li> <li>Writes <code>_gold_metadata.json</code> next to the data: table name, version, creation epoch, total files/rows.</li> <li>Returns <code>(total_files_read, gold_rows_written, manifest_path)</code>.</li> </ul>","path":["Components","Lakehouse Pipeline"],"tags":[]},{"location":"architecture/lakehouse-pipeline/#memory-budget-runtimes","level":2,"title":"Memory Budget &amp; Runtimes","text":"<p>All streaming reads/writes share one budget from <code>src/engine/memory.rs</code>:</p> Mechanism Value Per-stream row ceiling <code>BUDGET_BATCH_ROWS = 1 &lt;&lt; 20</code> Parallel scaling each of N streams gets <code>budget_batch_rows(N)</code> rows Total RAM cap <code>BASALTIC_RED_MAX_RAM_GB</code> env var, default 2 GB Safety factor ×3.5 for transient mask/clean/trash buffers <p>Two process-wide runtimes are created lazily: a multi-threaded tokio runtime (<code>global_runtime()</code>, used by DataFusion streams) and a sized Rayon pool (<code>global_rayon_pool(threads)</code>). <code>recommend_batch_size(parallel_streams)</code> exposes the computed per-stream row count if you need to pass an explicit <code>batch_size</code>.</p>","path":["Components","Lakehouse Pipeline"],"tags":[]},{"location":"architecture/matrix-engine/","level":1,"title":"MatrixEngine Core","text":"<p><code>MatrixEngine</code> (<code>src/engine/mod.rs</code>) is the single struct behind every command in the SDK. It carries the four data-quality thresholds used by the static fast-path filter and <code>preview_sample</code>:</p> <pre><code>pub struct MatrixEngine {\n    pub min_passenger: i64,   // default 1\n    pub max_passenger: i64,   // default 9\n    pub min_fare: f64,        // default 0.01\n    pub max_speed_mph: f64,   // default 100.0\n}\n</code></pre>","path":["Components","MatrixEngine Core"],"tags":[]},{"location":"architecture/matrix-engine/#construction","level":2,"title":"Construction","text":"<pre><code>import basaltic_red as br\n\n# Shared singleton behind all br.&lt;group&gt;.* commands (thresholds 1, 9, 0.01, 100.0)\nbr.read.slice_rows(...)\n\n# Custom thresholds for advanced use\nengine = br.MatrixEngine(\n    min_passenger=0,\n    max_passenger=20,     # raises ValueError if min &gt; max\n    min_fare=-5.0,\n    max_speed_mph=200.0,\n)\n</code></pre> <p><code>br.MatrixEngine(...)</code> accepts the same arguments positionally or by keyword. The shared instance is created once per process via a <code>OnceLock</code>, so thresholds never drift between sub-commands.</p>","path":["Components","MatrixEngine Core"],"tags":[]},{"location":"architecture/matrix-engine/#slicing-primitives","level":2,"title":"Slicing Primitives","text":"<p>Implemented in <code>src/engine/slice.rs</code>; exposed through <code>br.read.*</code>:</p> Method Behavior <code>slice_rows(file_path, offset, limit)</code> Reads one row range as a PyArrow Table. For Parquet it uses the row-group reader; for IPC/Feather it memory-maps via <code>memmap2</code>. <code>slice_cols(file_path, selected_cols, offset, limit)</code> Same, with column projection pushed into the reader (Parquet reads only the required column chunks). <code>preview_sample(file_path, limit_rows)</code> Opens the first batch only and runs the static threshold filter; returns <code>(clean_table, trash_table)</code>. <p>Both slice methods resolve the handler through the format registry, so they work on every supported format, not just Parquet.</p>","path":["Components","MatrixEngine Core"],"tags":[]},{"location":"architecture/matrix-engine/#error-taxonomy","level":2,"title":"Error Taxonomy","text":"<p>All engine failures flow through one enum — <code>BazanError</code> in <code>src/error.rs</code> — mapped to Python by a single function (<code>src/pyapi/mod.rs</code>):</p> Rust variant Python exception Typical cause <code>UnsupportedFormat(_)</code> <code>ValueError</code> Extension not registered and sniffing failed <code>DataFusion(_)</code> <code>RuntimeError</code> SQL parse/execution failure everything else (<code>Message</code>, IO, Arrow) <code>IOError</code> Missing files, empty files, corrupt data <p>Rule-syntax problems raised before execution are surfaced as <code>ValueError</code> directly from the rule parser.</p>","path":["Components","MatrixEngine Core"],"tags":[]},{"location":"architecture/matrix-engine/#static-vs-dynamic-filtering","level":2,"title":"Static vs Dynamic Filtering","text":"<ul> <li>Static fast path (<code>src/filter.rs</code> + <code>engine/filter.rs</code>): three fixed bit flags — passenger range, minimum fare, distance/fare anomaly — evaluated with Arrow compute kernels over whole columns (fully vectorized). Used by <code>process_batch</code> / <code>process_file</code> / <code>process_and_write_lake</code> / <code>preview_sample</code>. No rule parsing overhead; expects NYC-taxi-style columns (<code>passenger_count</code>, <code>fare_amount</code>, <code>trip_distance</code>) and ignores rows silently when a column is absent.</li> <li>Dynamic kernel (<code>engine/dynamic_filter.rs</code>): arbitrary user rules parsed from strings against any supported column type. See SIMD Bitmask Kernel.</li> </ul>","path":["Components","MatrixEngine Core"],"tags":[]},{"location":"architecture/overview/","level":1,"title":"Architecture Overview","text":"<p><code>basaltic-red</code> is a single Rust crate compiled as a Python extension (<code>cdylib</code>) via PyO3 + Maturin. Every public Python command routes through one shared engine instance into specialized Rust modules.</p>","path":["Components","Architecture Overview"],"tags":[]},{"location":"architecture/overview/#project-map","level":2,"title":"Project Map","text":"<p>Each component page names its source files inline. The table below is the entry map; the live tree always lives in the GitHub repo browser.</p> Component Location under <code>src/</code> Responsibility Docs Module entry <code>lib.rs</code> Registers submodules &amp; classes (<code>#[pymodule]</code>) — Error taxonomy <code>error.rs</code> <code>BazanError</code> variants mapped to Python exceptions MatrixEngine Core File discovery <code>utils.rs</code> Recursive, partition-aware file walkers Filtering Pipeline Static filter flags <code>filter.rs</code> Fixed-threshold audit bit flags MatrixEngine Core Python boundary <code>pyapi/</code> <code>br.*</code> namespaces, argument conversion, GIL release, shared engine Python API Reference Engine core <code>engine/mod.rs</code> <code>MatrixEngine</code> struct + quality thresholds MatrixEngine Core Dynamic kernel <code>engine/dynamic_filter.rs</code> Rule parsing + multi-chunk bitmask evaluation SIMD Bitmask Kernel Slicing <code>engine/slice.rs</code> Zero-copy row/column reads, sample preview MatrixEngine Core Parallel filter <code>engine/parallel_filter.rs</code>, <code>engine/partition.rs</code> Rayon multi-file filtering, Hive-style pruning Filtering Pipeline Format layer <code>engine/formats/</code> <code>FormatHandler</code> trait, registries, magic-byte sniffer Format Registry &amp; Sniffing SQL layer <code>engine/sql.rs</code>, <code>pyapi/iterator.rs</code> DataFusion session, <code>PyBatchIterator</code> bridge DataFusion SQL Layer Lake map <code>engine/map.rs</code> <code>.br_map.ipc</code> catalog + Lake Doctor Lake Map &amp; Lake Doctor Write pipeline <code>engine/ingest.rs</code>, <code>engine/splitter.rs</code>, <code>engine/formats/core/parquet.rs</code> Ingest, split, clean/trash lake write, gold table Lakehouse Pipeline Memory &amp; runtimes <code>engine/memory.rs</code> RAM budget, global tokio/Rayon runtimes Lakehouse Pipeline Extras <code>engine/csv_guard.rs</code>, <code>engine/graph.rs</code>, <code>engine/recommend.rs</code> CSV injection guard, Mermaid ER diagrams, batch-size hint —","path":["Components","Architecture Overview"],"tags":[]},{"location":"architecture/overview/#layered-execution-flow","level":2,"title":"Layered Execution Flow","text":"<pre><code>sequenceDiagram\n    autonumber\n    participant PY as Python br.*\n    participant API as pyapi (PyO3 boundary)\n    participant ENG as engine (MatrixEngine)\n    participant FMT as formats (FormatHandler)\n    participant OUT as PyArrow Table\n\n    PY-&gt;&gt;API: br.read.slice_rows(...)\n    note over API: convert args · map BazanError · release GIL\n    API-&gt;&gt;ENG: slice_rows_native()\n    ENG-&gt;&gt;FMT: handler_for(ext) or magic-byte sniff\n    FMT--&gt;&gt;ENG: OpenedSource (batch stream)\n    ENG--&gt;&gt;API: Arrow RecordBatch\n    API--&gt;&gt;PY: pyarrow.Table (zero-copy)</code></pre> <ol> <li>Python boundary (<code>pyapi/</code>) — converts arguments, maps <code>BazanError</code> to <code>PyValueError</code> / <code>PyRuntimeError</code> / <code>PyIOError</code>, and releases the GIL around native work via <code>py.detach</code>.</li> <li>Engine core (<code>engine/</code>) — owns all logic: format resolution, streaming reads, filtering, SQL planning.</li> <li>Format layer (<code>formats/</code>) — every file access resolves to a <code>FormatHandler</code> (extension lookup first, then magic-byte sniffing).</li> <li>Interop boundary — results cross back as PyArrow objects through Arrow's zero-copy interface; see DataFusion SQL Layer.</li> </ol>","path":["Components","Architecture Overview"],"tags":[]},{"location":"architecture/overview/#shared-engine-instance","level":2,"title":"Shared Engine Instance","text":"<p>All namespaced commands use one process-wide engine built by <code>default_engine()</code> in <code>src/pyapi/mod.rs</code>:</p> <pre><code>static DEFAULT_ENGINE: OnceLock&lt;MatrixEngine&gt; = OnceLock::new();\n// MatrixEngine::new(1, 9, 0.01, 100.0) — default quality thresholds\n</code></pre> <p>This guarantees identical validation thresholds across <code>br.read.*</code>, <code>br.filter.*</code>, <code>br.lake.*</code>, ... Custom thresholds are available by instantiating <code>br.MatrixEngine(min_passenger, max_passenger, min_fare, max_speed_mph)</code> directly.</p>","path":["Components","Architecture Overview"],"tags":[]},{"location":"architecture/overview/#memory-model","level":2,"title":"Memory Model","text":"<ul> <li>Streaming reads are capped at <code>BUDGET_BATCH_ROWS = 1 &lt;&lt; 20</code> rows per batch per stream.</li> <li>With N parallel streams each batch shrinks so total in-flight rows stay bounded — total RAM defaults to 2 GB, tunable via the <code>BASALTIC_RED_MAX_RAM_GB</code> environment variable.</li> <li>Details: Memory Budget &amp; Runtimes and <code>src/engine/memory.rs</code>.</li> </ul>","path":["Components","Architecture Overview"],"tags":[]},{"location":"architecture/simd-kernel/","level":1,"title":"Multi-Chunk Bitmask Kernel","text":"<p>The dynamic quality filter lives in <code>src/engine/dynamic_filter.rs</code>. It evaluates N arbitrary rules over a full <code>RecordBatch</code> without intermediate boolean arrays, writing results into raw <code>u64</code> bitmasks in place. Loops are plain Rust over Arrow arrays; LLVM auto-vectorizes the hot paths (no hand-written SIMD intrinsics).</p>","path":["Components","SIMD Bitmask Kernel"],"tags":[]},{"location":"architecture/simd-kernel/#rule-parsing","level":2,"title":"Rule Parsing","text":"<p><code>FilterRule::parse()</code> splits each rule string on the first operator found (longest-first so <code>&gt;=</code> wins over <code>&gt;</code>):</p> <pre><code>\"&lt;column&gt; &lt;op&gt; &lt;value&gt;\"   op ∈ { &gt;=, &lt;=, ==, !=, &gt;, &lt; }\n</code></pre> <ul> <li>Values may be quoted (<code>'</code> or <code>\"</code>) — quotes are trimmed before parsing.</li> <li>Numeric comparisons parse the right-hand side to the column's element type.</li> <li>String columns (<code>Utf8</code>, <code>LargeUtf8</code>) compare lexicographically.</li> </ul>","path":["Components","SIMD Bitmask Kernel"],"tags":[]},{"location":"architecture/simd-kernel/#evaluation-loop","level":2,"title":"Evaluation Loop","text":"<p>Each rule owns one bit position: rule i → chunk <code>i / 64</code>, bit <code>i % 64</code>. Rows violating any rule get that bit set and are marked not-clean:</p> <pre><code>for (rule_idx, rule) in rules.iter().enumerate() {\n    let chunk_idx = rule_idx / 64;\n    let bit = 1u64 &lt;&lt; (rule_idx % 64);\n    // per-column typed loop writes directly into error_chunks_raw[chunk_idx]\n}\n</code></pre> <p>Properties:</p> <ul> <li>Zero allocation in the inner loop — masks are preallocated once per batch (<code>Vec&lt;Vec&lt;u64&gt;&gt;</code>).</li> <li>Multi-chunk scaling — <code>num_chunks = ceil(rules / 64)</code>; 1 or 500+ rules work identically.</li> <li>NULL handling — null cells always fail the rule (row goes to Trash).</li> <li>Unknown column or unsupported dtype — the rule is silently skipped for that batch (no rows flagged by it).</li> </ul> <p>Supported column types: <code>Int8/16/32/64</code>, <code>UInt8/16/32/64</code>, <code>Float32/64</code>, <code>Utf8</code>, <code>LargeUtf8</code>.</p>","path":["Components","SIMD Bitmask Kernel"],"tags":[]},{"location":"architecture/simd-kernel/#audit-columns-on-the-trash-table","level":2,"title":"Audit Columns on the Trash Table","text":"<p>After evaluation, rows are split with <code>filter_record_batch</code> and the Trash batch is augmented:</p> Column Type Present when <code>audit_error_code</code> <code>UInt64</code> always — bitmask of violated rules 0–63 (first chunk) <code>audit_violated_rules</code> <code>List&lt;UInt32&gt;</code> only when rules &gt; 64 — list of all violated rule indices <p>Decoding <code>audit_error_code</code>: bit i set ⇔ rule i was violated, e.g. code <code>0b101</code> means rules 0 and 2 failed. For rule sets beyond 64 rules, read <code>audit_violated_rules</code> instead of relying on the single <code>UInt64</code>.</p> <p>Full encoding details: Audit Error Codes reference.</p>","path":["Components","SIMD Bitmask Kernel"],"tags":[]},{"location":"getting-started/concepts/","level":1,"title":"Core Concepts","text":"<p>Understanding the foundational building blocks of <code>basaltic-red</code>.</p>","path":["Getting Started","Core Concepts"],"tags":[]},{"location":"getting-started/concepts/#1-zero-copy-apache-arrow-architecture","level":2,"title":"1. Zero-Copy Apache Arrow Architecture","text":"<p><code>basaltic-red</code> uses Apache Arrow's standard in-memory columnar format across all operations. When data is read from Parquet or IPC files, raw byte buffers are parsed directly into Arrow <code>RecordBatch</code> structures. When handing data off to Python libraries (Polars, PyArrow, DuckDB), pointer transfers via the Arrow C Data Interface prevent redundant memory copying.</p> <pre><code>sequenceDiagram\n    participant D as Disk (Parquet / IPC)\n    participant R as Rust Arrow RecordBatch\n    participant A as PyArrow Table\n    participant PL as Polars DataFrame\n    participant DB as DuckDB Relation\n\n    D-&gt;&gt;R: memmap / FileReader parse\n    R-&gt;&gt;A: PyCapsule via Arrow C Data Interface\n    A--&gt;&gt;PL: zero-copy view\n    A--&gt;&gt;DB: zero-copy view</code></pre>","path":["Getting Started","Core Concepts"],"tags":[]},{"location":"getting-started/concepts/#2-multi-chunk-simd-bitmask-engine","level":2,"title":"2. Multi-Chunk SIMD Bitmask Engine","text":"<p>Traditional filtering evaluates rules row-by-row or creates intermediate boolean masks in memory. <code>basaltic-red</code> evaluates rules directly into bitwise memory buffers (<code>Vec&lt;u64&gt;</code>), updating bit flags in-place. - Arbitrary rule counts: Supports &gt;64 rules seamlessly across multiple 64-bit chunks. - Bitwise Audit Codes: Every rejected record in the Trash table is tagged with <code>audit_error_code</code> — a <code>UInt64</code> bitmask whose bit i marks rule i as violated. With more than 64 rules an additional <code>audit_violated_rules</code> list column records every violated index.</p>","path":["Getting Started","Core Concepts"],"tags":[]},{"location":"getting-started/concepts/#3-binary-lake-map-br_mapipc-lake-doctor","level":2,"title":"3. Binary Lake Map (<code>.br_map.ipc</code>) &amp; Lake Doctor","text":"<p>Rather than performing recursive filesystem walks across thousands of files on every query, <code>basaltic-red</code> maintains an Arrow IPC binary map (<code>.br_map.ipc</code>) inside the lake directory: - Contains relative paths, sizes, modification times, row counts, and per-column min/max stats. - Warm reads via <code>memmap2</code> (sub-millisecond in <code>demo.ipynb</code>; actual time depends on hardware/filesystem). - <code>br.lake.doctor</code> detects drift (unindexed, modified, missing files) and incrementally heals the catalog.</p>","path":["Getting Started","Core Concepts"],"tags":[]},{"location":"getting-started/install/","level":1,"title":"Installation &amp; Setup","text":"","path":["Getting Started","Installation"],"tags":[]},{"location":"getting-started/install/#prerequisites","level":2,"title":"Prerequisites","text":"<ul> <li>Python: 3.12+ (<code>requires-python = \"&gt;=3.12\"</code> in <code>pyproject.toml</code>)</li> <li>Rust Toolchain: stable <code>rustc</code> + <code>cargo</code> (automatically invoked when building via UV / Maturin)</li> <li>Package Manager: Recommended <code>uv</code> (fastest) or <code>pip</code></li> <li>Runtime dependency: <code>pyarrow</code> (installed automatically)</li> </ul>","path":["Getting Started","Installation"],"tags":[]},{"location":"getting-started/install/#1-install-directly-from-github-via-uv","level":2,"title":"1. Install Directly from GitHub via UV","text":"<p>Since <code>basaltic-red</code> is hosted on GitHub with a native Maturin PEP 517 build backend, you can install or add it directly with UV in a single command. UV will automatically fetch the repo, compile the Rust core, and install the package:</p> Add to UV ProjectInstall into Active VirtualenvWith Optional Dependencies (Polars, DuckDB, Notebook) <pre><code>uv add \"git+https://github.com/VanDung-dev/basaltic-red.git\"\n</code></pre> <pre><code>uv pip install \"git+https://github.com/VanDung-dev/basaltic-red.git\"\n</code></pre> <pre><code># Install with interop &amp; notebook support:\nuv add \"basaltic-red[interop,notebook] @ git+https://github.com/VanDung-dev/basaltic-red.git\"\n</code></pre>","path":["Getting Started","Installation"],"tags":[]},{"location":"getting-started/install/#2-install-from-local-source-development-mode","level":2,"title":"2. Install from Local Source (Development Mode)","text":"<p>If you have cloned the repository locally:</p> <ol> <li> <p>Clone repository:    <pre><code>git clone https://github.com/VanDung-dev/basaltic-red.git\ncd basaltic-red\n</code></pre></p> </li> <li> <p>Set up virtual environment with UV:    <pre><code>uv venv\nsource .venv/bin/activate  # On Linux/macOS\n# .venv\\\\Scripts\\\\activate  # On Windows\n</code></pre></p> </li> <li> <p>Install dependencies and compile:    <pre><code># Full dev setup (README quick start):\nuv sync --extra dev --extra interop\nuv run maturin develop --release\n\n# Or plain install from the local directory:\nuv pip install .\n</code></pre></p> </li> </ol>","path":["Getting Started","Installation"],"tags":[]},{"location":"getting-started/install/#3-verifying-installation","level":2,"title":"3. Verifying Installation","text":"<p>Run a quick Python command to verify that the PyO3 bindings and Rust SIMD engine are ready:</p> <pre><code>import basaltic_red as br\n\nprint(\"Basaltic-Red Version:\", br.__version__)\nprint(\"Available Modules:\", [m for m in dir(br) if not m.startswith(\"_\")])\n</code></pre> <p>Output: <pre><code>Basaltic-Red Version: 0.1.0\nAvailable Modules: ['MatrixEngine', 'PyBatchIterator', 'dictionary', 'filter', 'formats', 'graph', 'lake', 'read', 'sql']\n</code></pre></p>","path":["Getting Started","Installation"],"tags":[]},{"location":"getting-started/quickstart/","level":1,"title":"5-Minute Quickstart Guide","text":"<p>This guide walks you through the core workflow of <code>basaltic-red</code>: initializing a Data Lake, slicing data zero-copy, applying dynamic SIMD quality filters, and querying with SQL.</p>","path":["Getting Started","Quickstart Guide"],"tags":[]},{"location":"getting-started/quickstart/#step-1-initialize-data-lake-with-lake-doctor","level":2,"title":"Step 1: Initialize Data Lake with Lake Doctor","text":"<p>Always run <code>br.lake.doctor</code> when starting to work with a data directory:</p> <pre><code>import basaltic_red as br\n\n# Diagnose lake and generate .br_map.ipc catalog automatically\nhealth = br.lake.doctor(\"data\", auto_heal=True)\nprint(\"Lake Health Report:\")\nfor k, v in health.items():\n    print(f\"  - {k:18s}: {v}\")\n</code></pre>","path":["Getting Started","Quickstart Guide"],"tags":[]},{"location":"getting-started/quickstart/#step-2-zero-copy-sample-slicing","level":2,"title":"Step 2: Zero-Copy Sample Slicing","text":"<p>Read row and column slices from large Parquet files in microseconds:</p> <pre><code>import polars as pl\n\n# Slice rows [0..100] without reading the entire dataset\narrow_table = br.read.slice_rows(\"data/yellow_tripdata_2025-01.parquet\", offset=0, limit=100)\ndf = pl.from_arrow(arrow_table)\nprint(df.shape)  # (100, 20)\n</code></pre>","path":["Getting Started","Quickstart Guide"],"tags":[]},{"location":"getting-started/quickstart/#step-3-high-speed-simd-data-quality-filtering","level":2,"title":"Step 3: High-Speed SIMD Data Quality Filtering","text":"<p>Define arbitrary validation rules and partition records into Clean and Trash tables:</p> <pre><code>rules = [\n    \"passenger_count &gt; 0\",\n    \"trip_distance &gt; 0.0\",\n    \"fare_amount &gt;= 2.5\",\n    \"total_amount &gt; 0.0\",\n]\n\n# High-speed in-memory matrix filter\nclean_batch, trash_batch = br.filter.filter_matrix(\n    \"data/yellow_tripdata_2025-01.parquet\",\n    rules=rules\n)\n\nclean_df = pl.from_arrow(clean_batch)\ntrash_df = pl.from_arrow(trash_batch)\n\nprint(f\"Clean rows : {clean_df.height:,}\")\nprint(f\"Trash rows : {trash_df.height:,}\")\n</code></pre>","path":["Getting Started","Quickstart Guide"],"tags":[]},{"location":"getting-started/quickstart/#step-4-zero-copy-sql-analytics-with-datafusion","level":2,"title":"Step 4: Zero-Copy SQL Analytics with DataFusion","text":"<p>Stream query execution pushdown directly into Polars or DuckDB:</p> <pre><code>import duckdb\n\n# DataFusion SQL execution stream\nstream = br.sql.execute_sql_stream(\"SELECT passenger_count, AVG(fare_amount) AS avg_fare FROM 'data/output/clean_trips.parquet' GROUP BY passenger_count\")\n\n# Zero-copy handoff to DuckDB or Polars\nduck_df = duckdb.from_arrow(stream.to_pyarrow()).df()\nprint(duck_df)\n</code></pre>","path":["Getting Started","Quickstart Guide"],"tags":[]},{"location":"how-to/integrate-polars-duckdb/","level":1,"title":"Integrate with Polars &amp; DuckDB","text":"","path":["Guides","Integrate with Polars & DuckDB"],"tags":[]},{"location":"how-to/integrate-polars-duckdb/#integration-pattern-with-polars","level":2,"title":"Integration Pattern with Polars","text":"<pre><code>import polars as pl\nimport basaltic_red as br\n\n# Zero-copy slicing\narrow_table = br.read.slice_rows(\"data/yellow_tripdata_2025-01.parquet\", offset=0, limit=1000)\ndf = pl.from_arrow(arrow_table)\n</code></pre>","path":["Guides","Integrate with Polars & DuckDB"],"tags":[]},{"location":"how-to/integrate-polars-duckdb/#integration-pattern-with-duckdb","level":2,"title":"Integration Pattern with DuckDB","text":"<pre><code>import duckdb\nimport basaltic_red as br\n\n# DataFusion SQL execution to PyArrow Table\nstream = br.sql.execute_sql_stream(\"SELECT * FROM 'data/yellow_tripdata_2025-01.parquet'\")\nduck_df = duckdb.from_arrow(stream.to_pyarrow()).df()\n</code></pre>","path":["Guides","Integrate with Polars & DuckDB"],"tags":[]},{"location":"how-to/parallel-processing/","level":1,"title":"Multi-File Parallel Processing with Rayon","text":"<p><code>basaltic-red</code> uses Rayon's work-stealing thread pool to saturate NVMe drive bandwidth and CPU cores:</p> <pre><code>import basaltic_red as br\n\nrules = [\"passenger_count &gt; 0\", \"trip_distance &gt; 0.0\", \"fare_amount &gt;= 2.5\"]\n\n# Automatically scales across all available CPU threads\nsummary = br.filter.filter_files_parallel(\"data/yellow_tripdata_2025-*.parquet\", rules=rules)\n</code></pre>","path":["Guides","Multi-File Parallel Processing"],"tags":[]},{"location":"how-to/run-demo-notebook/","level":1,"title":"Run NYC Taxi Big Data Demo","text":"","path":["Guides","Run NYC Taxi Big Data Demo"],"tags":[]},{"location":"how-to/run-demo-notebook/#overview-of-demoipynb","level":2,"title":"Overview of <code>demo.ipynb</code>","text":"<p>The <code>demo.ipynb</code> notebook runs the full pipeline on the NYC Yellow Taxi 2009–2025 dataset (204 Parquet files, 29.66 GB, 1,826,960,642 rows — configurable <code>start_year</code>/<code>end_year</code> in the downloader cell):</p> <ol> <li>Step 0: Environment setup, Maturing build, workspace cleanup, configurable <code>target_year</code> downloader.</li> <li>Step 1: Lake Doctor initialization &amp; zero-copy schema extraction.</li> <li>Step 2: Slicing &amp; DataFusion SQL streaming.</li> <li>Step 3: Rayon SIMD parallel data quality filter.</li> <li>Step 4: DataFusion analytics &amp; publication Seaborn charts.</li> <li>Step 5: Magic byte sniffing &amp; &gt;64 rules scale test.</li> <li>Step 6: Final Lake Doctor integrity audit.</li> </ol>","path":["Guides","Run NYC Taxi Big Data Demo"],"tags":[]},{"location":"how-to/run-demo-notebook/#running-the-notebook","level":2,"title":"Running the Notebook","text":"<pre><code>uv run jupyter lab demo.ipynb\n</code></pre>","path":["Guides","Run NYC Taxi Big Data Demo"],"tags":[]},{"location":"how-to/troubleshooting/","level":1,"title":"Troubleshooting &amp; Optimization","text":"","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#common-issues-fixes","level":2,"title":"Common Issues &amp; Fixes","text":"","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#1-lake-status-reports-drift_detected","level":3,"title":"1. Lake Status reports <code>DRIFT_DETECTED</code>","text":"<ul> <li>Cause: Files were added, modified, or deleted without updating the catalog.</li> <li>Fix: Run <code>br.lake.doctor(\"data\", auto_heal=True)</code> — the report's <code>modified_files</code> / <code>unindexed_files</code> / <code>missing_files</code> keys tell you exactly what drifted.</li> </ul>","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#2-valueerror-unsupported-file-format-xyz","level":3,"title":"2. <code>ValueError: Unsupported file format '.xyz'</code>","text":"<ul> <li>Cause: The extension is not registered and magic-byte sniffing failed.</li> <li>Fix A: Check <code>br.formats.list_formats()</code> for supported extensions (<code>csv, tsv, psv, txt, json, jsonl, ndjson, parquet, pq, feather, arrow, ipc, avro, xlsx, orc, msgpack</code>).</li> <li>Fix B: Register a custom handler: <code>br.formats.register_delimited(ext=\"xyz\", delimiter=\"|\")</code>.</li> </ul>","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#3-ioerror-parquet-file-is-empty","level":3,"title":"3. <code>IOError: .parquet file is empty</code>","text":"<ul> <li>Cause: <code>preview_sample</code> / filtering opened a file whose first batch has zero rows.</li> <li>Fix: Remove or re-generate the empty file; run <code>br.lake.doctor</code> to find it.</li> </ul>","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#4-rule-seems-ignored-all-rows-pass","level":3,"title":"4. Rule seems ignored (all rows pass)","text":"<ul> <li>Cause: The rule references a column that doesn't exist, an unsupported dtype (dates, decimals…), or a value that can't be parsed into the column type — such rules are silently skipped per Rule Syntax.</li> <li>Fix: Verify column names via <code>br.read.slice_rows(path, 0, 1).schema</code>.</li> </ul>","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#5-sql-fails-on-json-files","level":3,"title":"5. SQL fails on JSON files","text":"<ul> <li>Cause: DataFusion expects newline-delimited objects; top-level <code>[...]</code> arrays take the MemTable fallback path.</li> <li>Fix: Prefer NDJSON for large SQL workloads.</li> </ul>","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#6-csv-formula-injection-warnings","level":3,"title":"6. CSV formula-injection warnings","text":"<ul> <li>Cause: String cells begin with <code>=</code>, <code>+</code>, <code>@</code>, or a non-numeric <code>-</code>.</li> <li>Behavior: The CSV Guard neutralizes these cells by prefixing <code>'</code> when writing CSV output; numeric negatives like <code>-5.0</code> pass through untouched (see <code>src/engine/csv_guard.rs</code>).</li> </ul>","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"how-to/troubleshooting/#optimization-tips","level":2,"title":"Optimization Tips","text":"<ul> <li>Partition pruning: keep Hive-style layouts (<code>year=.../month=.../</code>) and pass <code>partition_filter=</code> to skip whole directories before IO.</li> <li>RAM ceiling: raise/lower the budget with <code>BASALTIC_RED_MAX_RAM_GB</code> (default 2).</li> <li>Parallel width: cap workers with <code>num_threads=</code> on <code>filter_files_parallel</code> to leave cores for other jobs.</li> <li>Prefer Parquet: native ListingTable pushdown (predicate + projection) only applies to Parquet/delimited/JSON/IPC targets in SQL.</li> </ul>","path":["Guides","Troubleshooting & Optimization"],"tags":[]},{"location":"other/benchmarks/","level":1,"title":"Benchmarks","text":"<p>Measurements from a single run of <code>demo.ipynb</code> on the 17-year NYC TLC Yellow Taxi dataset (2009–2025, 204 Parquet files, 29.66 GB, 1,826,960,642 rows). Apple Silicon / macOS, <code>uv run --no-sync maturin develop --release</code>. Results vary by hardware and filesystem cache — indicative only.</p>","path":["Other","Benchmarks"],"tags":[]},{"location":"other/benchmarks/#1-dataset-size","level":2,"title":"1. Dataset size","text":"Metric Value Parquet files 204 (2009–2025) Columns 20 Rows 1,826,960,642 Cells 36,539,212,840 Disk size 29.66 GB (30,371.6 MB) <p>Counted via <code>pq.read_metadata</code> + <code>os.path.getsize</code> in the notebook; volume scan is metadata-only (no full data read).</p>","path":["Other","Benchmarks"],"tags":[]},{"location":"other/benchmarks/#2-catalog-inspection-br_mapipc","level":2,"title":"2. Catalog inspection (<code>.br_map.ipc</code>)","text":"<p><code>br.lake.doctor(\"data\", auto_heal=True)</code> on first call walks the directory and builds <code>.br_map.ipc</code>; warm calls read it via <code>memmap2</code>.</p> Mode Files Time (demo) Cold (build) 204 ~18,068 ms Warm (mmap, avg 5 runs) 204 ~0.5 ms <p>Warm avoids the directory walk. The exact speedup depends on storage and whether the map is already built.</p>","path":["Other","Benchmarks"],"tags":[]},{"location":"other/benchmarks/#3-full-lake-quality-filtering","level":2,"title":"3. Full-lake quality filtering","text":"<p><code>br.filter.filter_files_parallel(\"data/yellow_tripdata_*.parquet\", rules=[5 rules])</code> — Rayon parallel read + filter.</p> <p>Rules in demo: <code>passenger_count &gt;= 1</code>, <code>trip_distance &gt; 0.0</code>, <code>fare_amount &gt; 0.0</code>, <code>total_amount &gt; 0.0</code>, <code>total_amount &lt;= 1000.0</code>.</p> <pre><code>Total files : 204\nTotal rows  : 1,826,960,642\nClean       : 1,780,228,507\nTrash       : 46,732,135\nWall time   : ~21.1 s (from ExecuteTime 19:48:57.175 → 19:49:18.296)\n</code></pre> <p>Trash rows carry <code>audit_error_code</code> (first 64 rules as <code>u64</code> bitmask) and, when rules &gt; 64, <code>audit_violated_rules</code> (<code>List&lt;UInt32&gt;</code>).</p> <p>Filtering is plain Rust loops over Arrow arrays (LLVM auto-vectorizes); not hand-written SIMD intrinsics.</p>","path":["Other","Benchmarks"],"tags":[]},{"location":"other/benchmarks/#4-single-file-operations","level":2,"title":"4. Single-file operations","text":"<p>One monthly batch (<code>yellow_tripdata_2025-12.parquet</code>, ~4,305,006 rows) — used for the SQL and slicing demos:</p> <ul> <li>Slicing: <code>br.read.slice_rows(..., offset=0, limit=100)</code> returns in milliseconds (no full read).</li> <li>SQL: <code>br.sql.execute_sql_stream(\"SELECT ... GROUP BY passenger_count\")</code> — aggregation over the single file completes in ~0.1 s in the demo; handoff to DuckDB via <code>duckdb.from_arrow(stream.to_pyarrow())</code> is an Arrow FFI transfer.</li> </ul> <p>No claims about pandas/Postgres baselines — comparison depends on query and hardware.</p>","path":["Other","Benchmarks"],"tags":[]},{"location":"reference/audit-codes/","level":1,"title":"Audit Error Codes","text":"<p>When dynamic rules run (<code>filter_matrix</code>), every Trash row records which rules it violated.</p>","path":["Reference","Audit Error Codes & Schema"],"tags":[]},{"location":"reference/audit-codes/#audit_error_code-uint64-nullable","level":2,"title":"<code>audit_error_code</code> — <code>UInt64</code>, nullable","text":"<p>Bit i is set when rule i (0-indexed) was violated:</p> <p>$$\\text{audit_error_code} = \\sum_{i \\in \\text{violated},\\, i &lt; 64} 2^i$$</p> <p>Example: code <code>0b101</code> (=5) means rules 0 and 2 failed; rule 1 passed.</p> <p>Only the first 64 rules fit in the UInt64</p> <p>The column stores chunk 0 of the bitmask. With more than 64 rules, violations of rule ≥ 64 are not reflected in this column.</p>","path":["Reference","Audit Error Codes & Schema"],"tags":[]},{"location":"reference/audit-codes/#audit_violated_rules-listuint32-nullable","level":2,"title":"<code>audit_violated_rules</code> — <code>List&lt;UInt32&gt;</code>, nullable","text":"<p>Added to the Trash schema only when rules &gt; 64. Each row holds the full sorted list of violated rule indices across all chunks, e.g. <code>[0, 2, 71]</code>.</p>","path":["Reference","Audit Error Codes & Schema"],"tags":[]},{"location":"reference/audit-codes/#static-threshold-audit","level":2,"title":"Static-Threshold Audit","text":"<p>The static fast path (<code>process_batch</code>, <code>process_file</code>, <code>process_and_write_lake</code>, <code>preview_sample</code>) uses three fixed bit flags instead:</p> Flag Bit Condition <code>ERR_INVALID_PASSENGER</code> 1&lt;&lt;0 <code>passenger_count</code> null or outside <code>[min_passenger, max_passenger]</code> <code>ERR_INVALID_FARE</code> 1&lt;&lt;1 <code>fare_amount</code> null or below <code>min_fare</code> <code>ERR_INVALID_SPEED</code> 1&lt;&lt;2 fare violation and <code>trip_distance &gt; 0</code> (distance/fare anomaly) <p>The code is computed as a vectorized weighted sum (<code>p*1 + f*2 + s*4</code>) and stored as a non-null <code>UInt64</code> in static-mode Trash output. Missing columns contribute no violations.</p>","path":["Reference","Audit Error Codes & Schema"],"tags":[]},{"location":"reference/audit-codes/#clean-table-schema","level":2,"title":"Clean Table Schema","text":"<p>Unchanged — the Clean table always keeps the exact input schema with no extra columns.</p>","path":["Reference","Audit Error Codes & Schema"],"tags":[]},{"location":"reference/lake-map-spec/","level":1,"title":"Lake Map Specification","text":"<p>The Lake Map is serialized as an Apache Arrow IPC file named <code>.br_map.ipc</code> at the root of the data lake (<code>resolve_map_path()</code> in <code>src/engine/map.rs</code>). It is written by <code>br.lake.create_map()</code> and read back memory-mapped (&lt;1 ms) by <code>doctor_lake_map</code>.</p>","path":["Reference","Lake Map Specification"],"tags":[]},{"location":"reference/lake-map-spec/#recordbatch-schema-single-row-per-data-file","level":2,"title":"RecordBatch Schema (single row per data file)","text":"Column Arrow Type Nullable Description <code>rel_path</code> <code>Utf8</code> No File path relative to the lake root <code>size_bytes</code> <code>UInt64</code> No File size in bytes <code>mtime_ms</code> <code>Int64</code> No Modification time, milliseconds since Unix epoch <code>total_rows</code> <code>UInt64</code> No Row count of the file <code>stats_json</code> <code>Utf8</code> No JSON: <code>{\"total_rows\": N, \"columns\": {\"&lt;name&gt;\": {\"min\": f64, \"max\": f64, \"min_str\": str, \"max_str\": str}}}</code>","path":["Reference","Lake Map Specification"],"tags":[]},{"location":"reference/lake-map-spec/#doctor-comparison-keys","level":2,"title":"Doctor Comparison Keys","text":"<p>An entry is healthy when all three of <code>size_bytes</code>, <code>mtime_ms</code>, and existence match the current filesystem. Any mismatch classifies the file as <code>modified</code>; files present on disk but absent from the catalog are <code>unindexed</code>; catalog entries without a backing file are <code>missing</code>.</p>","path":["Reference","Lake Map Specification"],"tags":[]},{"location":"reference/lake-map-spec/#aggregate-totals","level":2,"title":"Aggregate Totals","text":"<p>The in-memory <code>LakeMap</code> struct additionally carries derived totals: <code>total_files</code>, <code>total_rows</code>, <code>total_bytes</code>.</p>","path":["Reference","Lake Map Specification"],"tags":[]},{"location":"reference/python-api/","level":1,"title":"Python API Reference","text":"<p>Signatures below match <code>src/pyapi/*.rs</code>. All functions return PyArrow objects (<code>Table</code> / <code>RecordBatch</code>) through the zero-copy bridge.</p>","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#top-level-exports","level":2,"title":"Top-level exports","text":"Name Kind <code>basaltic_red.MatrixEngine(min_passenger=1, max_passenger=9, min_fare=0.01, max_speed_mph=100.0)</code> class <code>basaltic_red.PyBatchIterator</code> class","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#basaltic_redread","level":2,"title":"<code>basaltic_red.read</code>","text":"Function Returns <code>slice_rows(file_path: str, offset: int, limit: int)</code> <code>pyarrow.Table</code> <code>slice_cols(file_path: str, selected_cols: list[str], offset: int, limit: int)</code> <code>pyarrow.Table</code> <code>preview_sample(file_path: str, limit_rows: int)</code> <code>(pyarrow.Table, pyarrow.Table)</code> — static-threshold clean/trash split of the first batch","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#basaltic_redfilter","level":2,"title":"<code>basaltic_red.filter</code>","text":"Function Returns <code>process_batch(batch: pyarrow.RecordBatch)</code> <code>(RecordBatch, RecordBatch)</code> — static-threshold clean/trash of one in-memory batch <code>process_file(file_path: str, batch_size: int)</code> <code>(total_rows, clean_rows, trash_rows)</code> <code>filter_matrix(file_path: str, rules: list[str])</code> <code>(pyarrow.Table, pyarrow.Table)</code> — dynamic rules; Trash gains audit columns <code>filter_files_parallel(path_pattern: str, rules: list[str], partition_filter: str \\| None = None, num_threads: int \\| None = None)</code> <code>dict</code> with keys <code>total_files</code>, <code>pruned_dirs</code>, <code>total_rows</code>, <code>clean_rows</code>, <code>trash_rows</code>","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#basaltic_redlake","level":2,"title":"<code>basaltic_red.lake</code>","text":"Function Returns <code>ingest(src_dir: str, dst_dir: str, auto_normalize: bool \\| None = None)</code> <code>(files_ingested, rows_ingested)</code> <code>split_file(file_path: str, max_rows_per_file: int, output_dir: str, format: str)</code> number of parts written <code>process_and_write_lake(input_dir: str, clean_output_dir: str, trash_output_dir: str, partition_filter: str \\| None, batch_size: int)</code> <code>(total_files, total_rows, clean_rows, trash_rows)</code> <code>generate_gold_table(input_dir: str, gold_output_dir: str, table_version: str, partition_filter: str \\| None, batch_size: int)</code> <code>(files_read, gold_rows, manifest_path)</code> <code>create_map(dir_path: str)</code> path to the written <code>.br_map.ipc</code> (<code>str</code>) <code>doctor(dir_path: str, auto_heal: bool = False)</code> dict — see Lake Doctor","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#basaltic_redsql","level":2,"title":"<code>basaltic_red.sql</code>","text":"Function Returns <code>execute_sql(query: str)</code> collected <code>pyarrow.Table</code> <code>execute_sql_stream(query: str)</code> <code>PyBatchIterator</code>","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#pybatchiterator","level":3,"title":"<code>PyBatchIterator</code>","text":"<ul> <li>iterable — yields PyArrow <code>RecordBatch</code> objects</li> <li><code>to_pyarrow()</code> → complete <code>pyarrow.Table</code></li> <li><code>repr()</code> → <code>PyBatchIterator(batches=N, rows=M)</code></li> </ul> <p>The <code>FROM '&lt;path&gt;'</code> target may be a file or directory. See DataFusion SQL Layer.</p>","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#basaltic_reddictionary","level":2,"title":"<code>basaltic_red.dictionary</code>","text":"Function Returns <code>export_data_dictionary_md(target_path: str, output_path: str)</code> Markdown content string (also written to <code>output_path</code>)","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#basaltic_redgraph","level":2,"title":"<code>basaltic_red.graph</code>","text":"Function Returns <code>generate_er_graph(path: str, output_path: str \\| None = None)</code> Mermaid ER diagram string (also written when <code>output_path</code> given); <code>path</code> may be a file or directory","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#basaltic_redformats","level":2,"title":"<code>basaltic_red.formats</code>","text":"Function Returns <code>register_delimited(ext: str, delimiter: str, has_header: bool = True)</code> registers a custom delimited handler (first byte of <code>delimiter</code> is used) <code>unregister_format(ext: str)</code> <code>bool</code> — whether a dynamic handler was removed <code>list_formats()</code> sorted <code>list[str]</code> of supported extensions","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/python-api/#error-mapping","level":2,"title":"Error Mapping","text":"Python exception Raised by <code>ValueError</code> unsupported format, invalid rule syntax, invalid engine arguments <code>RuntimeError</code> DataFusion SQL failures, stream errors <code>IOError</code> missing/empty/corrupt files and other IO problems","path":["Reference","Python API Reference"],"tags":[]},{"location":"reference/rule-syntax/","level":1,"title":"Rule Syntax Reference","text":"<p>Dynamic rules are plain strings parsed by <code>FilterRule::parse</code> (<code>src/engine/dynamic_filter.rs</code>):</p> <pre><code>&lt;column&gt; &lt;operator&gt; &lt;value&gt;\n</code></pre>","path":["Reference","Rule Syntax Reference"],"tags":[]},{"location":"reference/rule-syntax/#operators","level":2,"title":"Operators","text":"Operator Description Supported column types <code>&gt;</code> Greater than Int8/16/32/64, UInt8/16/32/64, Float32/64 <code>&gt;=</code> Greater than or equal same as <code>&gt;</code> <code>&lt;</code> Less than same as <code>&gt;</code> <code>&lt;=</code> Less than or equal same as <code>&gt;</code> <code>==</code> Equal numeric types + <code>Utf8</code>, <code>LargeUtf8</code> <code>!=</code> Not equal numeric types + <code>Utf8</code>, <code>LargeUtf8</code> <p>Parsing picks the first operator found, longest-first (<code>&gt;=</code> before <code>&gt;</code>), so whitespace placement is flexible: <code>\"age&gt;=18\"</code> and <code>\"age &gt;= 18\"</code> are equivalent.</p>","path":["Reference","Rule Syntax Reference"],"tags":[]},{"location":"reference/rule-syntax/#values","level":2,"title":"Values","text":"<ul> <li>Numeric rules parse the right-hand side to the column's element type (e.g. <code>\"fare_amount &gt;= 2.5\"</code>, <code>\"passenger_count &gt; 0\"</code>).</li> <li>String comparisons may be quoted — both <code>'N'</code> and <code>\"N\"</code> have quotes trimmed: <code>\"store_and_fwd_flag == 'N'\"</code>.</li> </ul>","path":["Reference","Rule Syntax Reference"],"tags":[]},{"location":"reference/rule-syntax/#evaluation-semantics","level":2,"title":"Evaluation Semantics","text":"Situation Result for the row Rule satisfied bit stays 0 → row remains Clean Rule violated or value is NULL bit set → row goes to Trash with <code>audit_error_code</code> Column missing from schema rule silently skipped (no rows flagged by it) Column type unsupported (dates, decimals, lists…) rule silently skipped for that batch Value fails to parse into the column's type rule silently skipped <p>Rules never mutate data; they only decide Clean vs Trash membership. See SIMD Bitmask Kernel for the encoding of violations.</p>","path":["Reference","Rule Syntax Reference"],"tags":[]},{"location":"workflows/data-quality-filtering/","level":1,"title":"Data Quality Filtering Workflow","text":"","path":["Guides","Data Quality Filtering"],"tags":[]},{"location":"workflows/data-quality-filtering/#multi-threaded-parallel-execution-with-rayon","level":2,"title":"Multi-Threaded Parallel Execution with Rayon","text":"<p>Filter dozens of Parquet files simultaneously across all CPU cores:</p> <pre><code>import basaltic_red as br\n\nrules = [\n    \"passenger_count &gt; 0\",\n    \"passenger_count &lt;= 6\",\n    \"trip_distance &gt; 0.0\",\n    \"fare_amount &gt;= 2.5\",\n    \"total_amount &gt; 0.0\",\n]\n\n# Parallel filter on all matching files\nsummary = br.filter.filter_files_parallel(\"data/yellow_tripdata_2025-*.parquet\", rules=rules)\nprint(f\"Files Evaluated : {summary['total_files']:,}\")\nprint(f\"Dirs Pruned     : {summary['pruned_dirs']:,}\")\nprint(f\"Total Evaluated : {summary['total_rows']:,}\")\nprint(f\"Clean Records   : {summary['clean_rows']:,}\")\nprint(f\"Trash Records   : {summary['trash_rows']:,}\")\n</code></pre>","path":["Guides","Data Quality Filtering"],"tags":[]},{"location":"workflows/data-quality-filtering/#optional-parameters","level":2,"title":"Optional Parameters","text":"<pre><code># Restrict to a Hive-style partition subtree and cap worker threads\nsummary = br.filter.filter_files_parallel(\n    \"data/lakehouse\",\n    rules=rules,\n    partition_filter=\"year=2026/month=08\",\n    num_threads=8,\n)\n</code></pre> <p><code>path_pattern</code> accepts a single file, a directory (walked recursively), or a glob. See Filtering Pipeline for internals. Note this command counts clean/trash rows; use <code>br.lake.process_and_write_lake</code> to materialize outputs.</p>","path":["Guides","Data Quality Filtering"],"tags":[]},{"location":"workflows/lake-doctor/","level":1,"title":"Lake Doctor &amp; Self-Healing Workflow","text":"","path":["Guides","Lake Doctor & Self-Healing"],"tags":[]},{"location":"workflows/lake-doctor/#operational-life-cycle","level":2,"title":"Operational Life Cycle","text":"<p>The Lake Doctor is the mandatory entrypoint and exit point for robust data lake pipelines:</p> <pre><code>sequenceDiagram\n    autonumber\n    participant P as Data Pipeline\n    participant D as Lake Doctor\n\n    P-&gt;&gt;D: entry check — doctor(\"data\", auto_heal=True)\n    D--&gt;&gt;P: HEALTHY baseline · catalog ready\n    P-&gt;&gt;P: ingest · slicing · SIMD filters · SQL\n    P-&gt;&gt;D: exit check — doctor(\"data\", auto_heal=True)\n    D--&gt;&gt;P: 100% integrity · zero orphaned state</code></pre>","path":["Guides","Lake Doctor & Self-Healing"],"tags":[]},{"location":"workflows/lake-doctor/#example-code","level":2,"title":"Example Code","text":"<pre><code>import basaltic_red as br\n\n# Diagnostic run\nstatus = br.lake.doctor(\"data\", auto_heal=False)\nif status[\"status\"] != \"HEALTHY\":\n    print(\"Drift detected! Healing lake...\")\n    br.lake.doctor(\"data\", auto_heal=True)\n</code></pre>","path":["Guides","Lake Doctor & Self-Healing"],"tags":[]},{"location":"workflows/partitioning/","level":1,"title":"Clean &amp; Trash Partitioning Workflow","text":"<p>When data quality filtering executes, records are separated into two distinct destination tables:</p>","path":["Guides","Clean & Trash Partitioning"],"tags":[]},{"location":"workflows/partitioning/#1-clean-table","level":2,"title":"1. Clean Table","text":"<p>Contains all records that satisfy 100% of the validation rules. Retains the exact original schema.</p>","path":["Guides","Clean & Trash Partitioning"],"tags":[]},{"location":"workflows/partitioning/#2-trash-table","level":2,"title":"2. Trash Table","text":"<p>Contains all anomalous records that violated at least one rule. Automatically augmented with audit columns: - <code>audit_error_code</code> (<code>UInt64</code>): Bitmask where the $i$-th bit is $1$ if rule $i$ was violated (first 64 rules). - <code>audit_violated_rules</code> (<code>List&lt;UInt32&gt;</code>, only when rules &gt; 64): full list of violated rule indices.</p> <pre><code>clean_batch, trash_batch = br.filter.filter_matrix(\"data/sample.parquet\", rules=rules)\n</code></pre> <p>Encoding details: Audit Error Codes.</p>","path":["Guides","Clean & Trash Partitioning"],"tags":[]},{"location":"workflows/partitioning/#writing-cleantrash-trees-on-disk","level":2,"title":"Writing Clean/Trash Trees on Disk","text":"<p><code>br.lake.process_and_write_lake</code> splits every Parquet file under a directory into two mirrored Parquet trees using the static engine thresholds (not custom rules):</p> <pre><code>stats = br.lake.process_and_write_lake(\n    \"input_dir\", \"output/clean/\", \"output/trash/\",\n    partition_filter=None, batch_size=65536,\n)\n# → (total_files, total_rows, clean_rows, trash_rows)\n</code></pre> <p>Trash files carry the static <code>audit_error_code</code>; output is ZSTD-compressed and preserves relative layout.</p>","path":["Guides","Clean & Trash Partitioning"],"tags":[]},{"location":"workflows/sql-analytics/","level":1,"title":"Zero-Copy SQL Analytics Workflow","text":"","path":["Guides","Zero-Copy SQL Analytics"],"tags":[]},{"location":"workflows/sql-analytics/#stream-pushdown-integration","level":2,"title":"Stream Pushdown Integration","text":"<p>Execute SQL aggregations directly on clean Parquet output files and pass batches to Polars:</p> <pre><code>import polars as pl\nimport basaltic_red as br\n\n# DataFusion SQL execution stream\nstream = br.sql.execute_sql_stream(\"\"\"\n    SELECT \n        passenger_count, \n        COUNT(*) AS total_trips,\n        ROUND(AVG(fare_amount), 2) AS avg_fare,\n        ROUND(SUM(total_amount), 2) AS total_revenue\n    FROM 'data/output/clean_trips.parquet'\n    GROUP BY passenger_count\n    ORDER BY total_trips DESC\n\"\"\")\n\n# Convert to Polars DataFrame without memory copy\ndf = pl.from_arrow(stream.to_pyarrow())\nprint(df)\n</code></pre>","path":["Guides","Zero-Copy SQL Analytics"],"tags":[]}]}